## ----globals -------------------------------------------------------------
packages <- c("kable", "kableExtra",
            "eval.save")
lapply(packages, require, character.only=T)

## ----load.data -------------------------------------------------------------

